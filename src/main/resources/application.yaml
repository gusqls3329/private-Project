spring:
  config:
    activate:
      on-profile: "BeenProject"
  datasource:
    url: jdbc:mariadb://localhost:3306/beenProject
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true